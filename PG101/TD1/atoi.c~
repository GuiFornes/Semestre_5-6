#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[])
{
  if (argc != 2){
    printf("Usage: %s argument-a-afficher\n",argc);
    return 1;
  }
  char * str = argv[1];
  int n = atoi(str);
  int t[n];
  printf("%d/n", n);
  return 0;
}
